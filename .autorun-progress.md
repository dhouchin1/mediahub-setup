# Autorun progress — mediahub-setup

Branch: `autorun/headless-dryrun-doctor` (off `main` @ b2696b3).
Theme: round out the headless / unattended-ops + lifecycle surface that landed
with the seedbox topology work. **Thread complete** as of 2026-06-03.

## Shipped (all on the feature branch, tree green throughout)
- `5561945` **install --dry-run** — validate config + preflight, build
  settings, print the resolved plan (services/ports/wiring) and stop before
  Docker. Cloud-init pre-check.
- `a8ce2fb` **doctor** — read-only health check (Docker daemon, container
  states, disk headroom, Tailscale for server roles). Exit 0/1/2/3. New
  `doctor.py`. Verified against the live local deployment.
- `ab30298` **down** — `docker compose down` from the CLI; volumes preserved
  by default, `--volumes` wipes (confirm-gated). New `docker_ops.compose_down()`.
- `5ae6455` **docs** — README "Operating a deployment (CLI)" + REMOTE-SEEDBOX
  dry-run/doctor notes.
- `1cc1f7e` **backup / restore** — tarball config+compose+.env (media
  excluded); safe extraction (data filter + traversal guard, --force gate).
  New `backup.py`. Roadmap item ticked.
- `39a2c57` **doctor --json** — machine-readable report for monitoring; same
  exit codes.

Final suite at thread close: **343 passed / 1 skipped, ruff clean.**

## Not done (next themes — deliberately left)
- Roadmap remainders are a *different* theme and out of this thread's scope:
  Windows/WSL2 support (large platform effort), xterm.js install-log widget
  (frontend), Notifiarr → Discord/ntfy/Slack routing (needs product decisions
  on which targets). Plex was already added pre-run.
- Surfacing `doctor`/`backup` in the web dashboard = product-taste (button
  placement, destructive-action UX in a browser) → HITL.

## HITL / observations (not acted on)
- The user's **live** local deployment had `mediahub-bazarr` and
  `mediahub-jellyfin` **Exited (137)** (~3h, likely OOM-killed). Surfaced by
  `doctor`, left untouched (belongs to the running deployment, out of scope).
  Worth a look: `docker logs mediahub-jellyfin` / raise container mem limits.
